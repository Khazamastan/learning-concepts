# Testing Notes

- Uses React Testing Library to simulate user interactions.
- Waits for the asynchronous success message to appear, matching the alert text from the vanilla form.
- Import path assumes `RegistrationForm.jsx` sits under `docs/forms` when copied into a project structure (update relative path as needed).
