# Registration Form Demo

## Features
- Collects text, email, select, and radio inputs.
- Serializes data via `FormData` and renders JSON preview.
- Alerts the user with "Form Submitted Successfully" to align with test expectation.

## Running the Demo
Open `registration-form.html` in a browser. Submit the form to see serialized output in the `<pre>` block.
