# Build a Multi Step Form

Three-step wizard collecting personal info, contact info, and a review screen before submission.

## Features

- Progress indicator highlights current/completed steps.
- Navigation buttons (Back/Next) with final submit action.
- Review step shows JSON summary before submission.

## Run locally

```bash
cd multi-step-form
npm install
npm run dev
```

Open `http://localhost:5173`.
